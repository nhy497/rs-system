# RS-System v3.1 升級指南

本文檔說明如何使用新增的功能和開發工具。

---

## ✨ v3.1 新增功能

### 1. 📡 跨標籤頁即時同步 (PLAN-A1)

**功能說明**：
- 當你在一個標籤頁保存課堂記錄時，其他標籤頁會自動更新
- 使用 BroadcastChannel API 實現
- 無需手動刷新頁面

**使用方式**：
```javascript
// 自動啟用，無需設置
// 當保存課堂記錄時，系統會自動通知其他標籤頁
```

**瀏覽器支援**：
- Chrome 54+
- Firefox 38+
- Safari 15.4+
- Edge 79+

**如果瀏覽器不支援**：系統會自動降級至傳統模式，不會影響基本功能。

---

### 2. 🔧 Creator 權限優化 (PLAN-A2)

**更改說明**：
- Creator 現在可以以「測試模式」新增課堂記錄
- 保存按鈕會顯示「💾 儲存（Creator 測試模式）」
- 不再完全禁止 Creator 新增功能

**原因**：Creator 需要測試新增功能，完全禁止過於嚴格。

---

### 3. 🛠️ Vite 構建系統 (PLAN-D1)

**安裝依賴**：
```bash
npm install
```

**開發模式**：
```bash
npm run dev
# 瀏覽器會自動打開 http://localhost:3000
```

**構建生產版**：
```bash
npm run build
# 輸出目錄：dist/
```

**預覽構建結果**：
```bash
npm run preview
```

**優勢**：
- ⭐ 極速熱更新（HMR）
- ⭐ 代碼分割優化
- ⭐ 自動壓縮和打包
- ⭐ 現代化開發體驗

---

### 4. 🚀 GitHub Actions CI/CD (PLAN-D3)

**自動化流程**：
1. ✅ 推送到 `main` 分支時自動觸發
2. 🛠️ 自動執行 ESLint 檢查
3. 🧪 自動執行測試
4. 🛠️ 構建生產版
5. 🚀 部署到 GitHub Pages

**查看構建狀態**：
- 訪問: https://github.com/nhy497/rs-system/actions
- 查看最近的 workflow 運行結果

**部署網址**：
- https://nhy497.github.io/rs-system/

---

### 5. 🎨 UI 組件系統 (PLAN-B4)

#### Toast 通知組件

**使用方式**：
```javascript
import { toast } from './src/components/Toast.js';

// 成功通知
toast.success('✅ 儲存成功！');

// 錯誤通知
toast.error('❌ 儲存失敗');

// 警告通知
toast.warning('⚠️ 請注意');

// 信息通知
toast.info('ℹ️ 會話已過期');

// 自定義持續時間
toast.success('已儲存', 5000); // 5秒
```

**功能**：
- ✨ 支持 4 種類型：success, error, warning, info
- ✨ 自動關閉（預設 3 秒）
- ✨ 手動關閉按鈕
- ✨ 佇列管理（多個通知依次顯示）
- ✨ 動畫效果

#### Modal 彈窗組件

**使用方式**：
```javascript
import { modal } from './src/components/Modal.js';

// 確認對話框
modal.confirm('確定要刪除嗎？', '確認刪除', () => {
  console.log('用戶點擊了確認');
});

// 警告框
modal.alert('操作完成！', '提示');

// 自定義 Modal
modal.show({
  title: '課堂詳情',
  content: '<p>課堂內容...</p>',
  confirmText: '關閉',
  showFooter: false
});
```

**功能**：
- ✨ 可自定義標題、內容、按鈕
- ✨ 點擊背景關閉
- ✨ ESC 鍵關閉
- ✨ 動畫效果
- ✨ 自動管理背景滾動

---

## 📚 開發指南

### 專案結構

```
rs-system/
├── src/                    # 源代碼目錄
│   ├── components/        # UI 組件
│   │   ├── Toast.js       # Toast 通知
│   │   └── Modal.js       # Modal 彈窗
│   ├── modules/           # 業務模組（未來擴展）
│   └── utils/             # 工具函數（未來擴展）
├── .github/
│   └── workflows/
│       └── deploy.yml     # CI/CD 配置
├── dist/                  # 構建輸出目錄
├── index.html             # 主頁
├── system.js              # 核心邏輯
├── styles.css             # 樣式表
├── package.json           # 項目配置
├── vite.config.js         # Vite 配置
└── README.md              # 項目說明
```

### 開發命令

```bash
# 安裝依賴
npm install

# 開發模式（熱更新）
npm run dev

# 構建生產版
npm run build

# 預覽構建結果
npm run preview

# 代碼檢查
npm run lint

# 自動修復代碼問題
npm run lint:fix

# 執行測試
npm test

# 測試 UI
npm run test:ui

# 代碼格式化
npm run format
```

---

## 🔧 常見問題

### Q1: Vite 構建失敗？

**解決方案**：
```bash
# 1. 刪除 node_modules 和 package-lock.json
rm -rf node_modules package-lock.json

# 2. 重新安裝
npm install

# 3. 再次嘗試構建
npm run build
```

### Q2: GitHub Actions 部署失敗？

**檢查項目**：
1. 確認 GitHub Pages 已啟用
2. 確認分支設置為 `main`
3. 查看 Actions 日誌：https://github.com/nhy497/rs-system/actions

### Q3: 跨標籤頁同步不生效？

**原因**：
- 瀏覽器不支援 BroadcastChannel
- 開啟了隱私模式（Private Browsing）

**解決方案**：
- 更新瀏覽器到最新版本
- 使用普通模式（非隱私模式）

---

## 🚀 後續計劃

### 已完成 ✅
- [x] PLAN-A1: 跨標籤頁同步
- [x] PLAN-A2: Creator 權限優化
- [x] PLAN-B4: UI 組件系統 (Toast + Modal)
- [x] PLAN-D1: Vite 構建系統
- [x] PLAN-D3: GitHub Actions CI/CD

### 待實施 🚧
- [ ] PLAN-A3: 數據壓縮優化 (LZ-String)
- [ ] PLAN-B1: 模組化重構 Phase 1
- [ ] PLAN-B2: 虛擬滾動列表
- [ ] PLAN-B3: IndexedDB 混合存儲
- [ ] PLAN-C2: 離線優先 PWA
- [ ] PLAN-D2: 測試框架
- [ ] PLAN-D4: ESLint + Prettier

---

## 📝 版本記錄

### v3.1.0 (2026-02-12)

**新增**
- ✨ BroadcastChannel 跨標籤頁即時同步
- ✨ Vite 構建系統
- ✨ GitHub Actions CI/CD 自動部署
- ✨ Toast 通知組件
- ✨ Modal 彈窗組件

**優化**
- 🔧 Creator 權限調整，允許測試模式新增課程
- 🔧 改善備份機制（版本 3.1）

**修復**
- 🐛 修復多標籤頁數據不同步問題

---

## 👥 貢獻

歡迎提交 Issue 和 Pull Request！

**聯絡方式**：
- GitHub: https://github.com/nhy497/rs-system
- Email: nhy040907@gmail.com

---

## 📝 授權

MIT License - 詳見 LICENSE 檔案
