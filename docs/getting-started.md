# 🚀 快速開始

## 先決條件

- Node.js >= 18.0.0
- npm >= 9.0.0

## 安裝步驟

### 1. Clone 專案

```bash
git clone https://github.com/nhy497/rs-system.git
cd rs-system
```

### 2. 安裝依賴

```bash
npm install
```

### 3. 啟動開發伺服器

```bash
npm run dev
```

開啟瀏覽器訪問 `http://localhost:5173`

## 執行測試

### 單元測試

```bash
# 執行所有測試
npm test

# 測試覆蓋率
npm run test:coverage

# 測試 UI 界面
npm run test:ui
```

### 手動測試

開啟瀏覽器訪問以下測試頁面：

```bash
# 啟動開發伺服器
npm run dev

# 訪問測試頁面
http://localhost:5173/tests/manual/test-modules.html
http://localhost:5173/tests/manual/test-save-refresh.html
http://localhost:5173/tests/manual/system-test.html
```

## 建置與預覽

```bash
# 建置生產版本
npm run build

# 預覽建置結果
npm run preview
```

預覽地址：`http://localhost:4173/rs-system/`

## 第一次使用

### 1. 訪問系統

開啟瀏覽器訪問 `http://localhost:5173`

### 2. 登入系統

預設 Creator 帳號：
- **用戶名**: `creator`
- **密碼**: `1234`

### 3. 建立課堂記錄

1. 點擊「新增記錄」按鈕
2. 填寫課堂資訊（日期、班級、學生人數）
3. 選擇學生完成的技能
4. 添加備註（選填）
5. 點擊「保存」

### 4. 查看記錄

- **所有記錄**: 點擊側邊欄「記錄列表」
- **按班別查看**: 使用篩選功能
- **匯出 CSV**: 點擊「匯出」按鈕

## 快速參考

### 常用指令

```bash
# 開發
npm run dev          # 啟動開發伺服器
npm test            # 執行測試
npm run lint        # 代碼檢查
npm run format      # 代碼格式化

# 建置
npm run build       # 建置生產版本
npm run preview     # 預覽建置結果
```

### 預設帳號

| 用戶名 | 密碼 | 角色 | 權限 |
|--------|------|------|------|
| creator | 1234 | Creator | 完整系統權限 |

### 儲存位置

- **本地儲存**: localStorage
- **資料庫**: IndexedDB (`rs-system-{userId}`)

### 資料備份

系統使用雙重備份機制：
- **localStorage**: 即時儲存
- **IndexedDB**: 持久化儲存

## 疑難排解

### 無法登入

1. 檢查用戶名和密碼是否正確
2. 清除瀏覽器 localStorage: `localStorage.clear()`
3. 重新載入頁面

### 測試無法執行

1. 確保已安裝依賴: `npm install`
2. 檢查 Node.js 版本: `node --version` (需要 >= 18.0.0)
3. 清除快取: `rm -rf node_modules && npm install`

### 建置失敗

1. 檢查依賴是否完整: `npm install`
2. 清除建置快取: `rm -rf dist`
3. 重新建置: `npm run build`

## 下一步

- 閱讀 [architecture.md](./architecture.md) 了解系統架構
- 閱讀 [development.md](./development.md) 了解開發規範
- 閱讀 [testing.md](./testing.md) 了解測試流程
- 閱讀 [user-guide.md](./user-guide.md) 了解功能使用

---

**提示**: 如有任何問題，請查看 [GitHub Issues](https://github.com/nhy497/rs-system/issues) 或創建新 Issue。
